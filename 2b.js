// Define a constant variable 'y' and assign it an arrow function that takes 'myVar' as a parameter
const y = (myVar) => {
    // Log the value of 'myVar' to the console
    console.log(myVar)
}

// Call the function 'y' with the argument "Hello" to execute the code inside it
y("Hello")
